<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Wake Lock：持有 PARTIAL_WAKE_LOCK 的进程会被系统 Cached App Freezer 跳过（豁免冻结），
         这是防止后台 API 服务被 MIUI/原生冻结器停摆的关键手段。 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 电池优化白名单：引导用户把 APP 加入忽略电池优化列表（MIUI 省电策略 → 无限制）。 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- 看门狗精确闹钟：Android 12+ 需显式声明才允许在 Doze 下把进程唤醒，
         用于在 ColorOS 强杀前台服务后把它重新拉起来。 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- 开机自启：系统重启后把服务拉回来，否则每次重启手机本地 API 都会失联。 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 悬浮窗（可选保活）：可见窗口会让进程处于 VISIBLE 级别，LMK 回收时排在前台服务之后。
         注意它只能"降低被杀概率"，挡不住厂商的一键清理/速冻，详见设置页说明。
         需 SYSTEM_ALERT_WINDOW 运行时授权。 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".GrokApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Grok2API"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OAuthWebActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".FarmSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".FarmActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />
        <service
            android:name=".ApiHostService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-started native on-device API server" />
        </service>
        <!-- 产号定时补号自检（每小时 AlarmManager 唤醒，检查池可用率） -->
        <receiver
            android:name=".FarmTopUpReceiver"
            android:enabled="true"
            android:exported="false" />
        <!-- 开机自启接收器：用户在 App 里开着服务时，系统重启后自动恢复。 -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
